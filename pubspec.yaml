name: devocional_plus
description: Aplicativo cristão Devocional+
publish_to: 'none'
version: 1.0.31+32

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.2.0
  package_info_plus: ^8.0.0
  url_launcher: ^6.3.0
  shared_preferences: ^2.3.2
  path_provider: ^2.1.4
  flutter_app_installer: ^2.0.0
  flutter_local_notifications: ^17.2.2
  timezone: ^0.9.4
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo.png
